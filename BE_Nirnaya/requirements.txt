# ── Core Framework ──────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── Supabase ─────────────────────────────────────────────────────────────────
supabase>=2.10.0

# ── Redis (Upstash compatible — async) ───────────────────────────────────────
redis[hiredis]>=5.0.0

# ── LLM — OpenAI ─────────────────────────────────────────────────────────────
openai>=1.0.0

# ── LLM — Anthropic (direct SDK) ─────────────────────────────────────────────
anthropic>=0.20.0

# ── LLM — AWS Bedrock (Anthropic via Bedrock API key) ────────────────────────
boto3>=1.26.0
botocore>=1.29.0

# ── Structured Logging ────────────────────────────────────────────────────────
structlog>=24.0.0

# ── Cryptography (AES-256-GCM for session API key encryption) ─────────────────
cryptography>=42.0.0

# ── LangChain & Agents ────────────────────────────────────────────────────────
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0
langchain-aws>=0.2.0

# ── LangGraph (orchestrator + worker graphs, interrupt/resume checkpointing) ───
langgraph>=0.2.0
langgraph-checkpoint-postgres>=2.0.0

# ── Psycopg3 async (required by langgraph-checkpoint-postgres) ────────────────
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# ── HTTP client (shared dep — let pip resolve the right version) ─────────────
httpx>=0.23.0,<0.28

# ── File Processing (CSV, Parquet, Excel) ─────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0
pyarrow>=14.0.0
python-multipart>=0.0.9

qstash>=2.0.0